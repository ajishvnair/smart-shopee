import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    // paddingTop: 40,
    alignItems: "center",
    flexDirection: "row",
    marginHorizontal: 10,
  },
  picker: {
    flex: 1,
  },
});
export default styles;
